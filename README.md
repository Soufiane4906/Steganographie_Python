# Steganographie_Python
Mini Projet Python qui démontre la stéganographie via une GUI qui cache un message écrit par l'utilisateur dans une image 

Prerequis :
pip install stegano
pip install tk 
pip install Image
pip install PILLOW
